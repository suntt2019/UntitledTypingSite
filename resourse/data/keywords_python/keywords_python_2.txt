except del is nonlocal class assert else True lambda None assert for import True yield else raise as in continue not return False None not break for del for pass is del and False try global or lambda 