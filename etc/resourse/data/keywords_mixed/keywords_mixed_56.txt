try default reinterpret_cast do operator foreach if double require eval() function friend not_eq private public enum endwhile return elseif while final double do signed double register static_cast goto 