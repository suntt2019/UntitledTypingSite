module void for assert enum new var int final case interface public continue final abstract throw float private catch for final instanceof const synchronized boolean this const volatile if goto char enum 